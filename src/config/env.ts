/**
 * @deprecated Prefer env.public.ts (client) or env.server.ts (API/routes).
 * Kept for backward compatibility during migration.
 */
export { publicEnv as env, hasSupabaseConfig } from "./env.public";
